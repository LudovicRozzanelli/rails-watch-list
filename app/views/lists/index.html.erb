<div class="container">
<h1>All List</h1>

<div>
  <% @lists.each do |list| %>
    <%=link_to list.name, list_path(list) %>
  <% end %>
</div>


<%= link_to "New list", new_list_path, class:"btn btn-primary mb-3" %>
